import { padding } from "../styles"

export default function About () {
    return (
        <section className={`${padding.XY} relative w-full h-screen`}>
            <p className="text-6xl font-extrabold">About Me</p>
        </section>
    )
}